/**
 * Every plate in this set is the same hour: dawn over open steppe, the sky
 * still cold, the ground already catching the first warm light. That is
 * literally what «Аркаим» looks like from above at 5 a.m. — so the palette
 * doesn't invent a mood, it reads it off the photographs. Type takes the
 * warm half of that sky, never a default accent.
 */
export const theme = {
  bg: "#0a0d16",
  panel: "#171a23",
  panelLight: "#242a35",
  border: "#3a3628",

  accent: "#e0964a",
  accentLight: "#ffcf8e",
  accentDeep: "#8a4d16",

  /* the cold half of the same dawn sky — structure only, never type */
  cold: "#4a6a8c",

  text: "#f6f1e6",
  muted: "#a89e88",
};
